# tralafiti's dotfiles

## Installation (Git Read-Only)

```
git clone git://github.com/tralafiti/dotfiles.git ~/.dotfiles
~/.dotfiles/init.sh
```

The git-hooks will link all hidden dotfiles into your home directory.
